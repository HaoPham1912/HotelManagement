server.port= 9090

spring.datasource.url=jdbc:mysql://localhost:3306/bookinghotel?useSSL=false
spring.datasource.username=root
spring.datasource.password=1234qwer
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl = true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql = true

spring.datasource.platform = mysql
spring.datasource.initialization-mode=never
spring.datasource.schema=classpath:data/data-import.sql

JWT_SECRET=SecretForAccessToken
JWT_REFRESH_SECRET=SecretForRefreshToken
#JWT_EXPIRATION=604800000L
#JWT_REFRESH_EXPIRATION=604800000L


##Step 1: init database if not exist (empty database)
##Step2 spring.jpa.hibernate.ddl-auto = create and comment line
# spring.datasourSSce.initialization-mode=never (create schema)
##Step3 Run project => stop
##Step4 spring.jpa.hibernate.ddl-auto= none and uncomment
# spring.datasource.initialization-mode=always (no change schema and import data source)
##Re run project
## after import success stop project and change  spring.datasource.initialization-mode=
# never to prevent re import duplicate data
##done